import { ValidationFormMessageDirective } from './validation-form-message.directive';

describe('ValidationFormMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationFormMessageDirective();
    expect(directive).toBeTruthy();
  });
});
